<h1>Listing People</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.id %></td>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td class="do_something"><%= link_to 'Display', user %></td>
    <td class="do_something"><%= link_to 'Edit', edit_user_path(user) %></td>
    <td class="do_something"><%= link_to 'Annihilate', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
